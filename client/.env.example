# Publiczny adres wdrożonego backendu. W developmencie można zostawić puste
# (domyślnie http://localhost:3000). Na Netlify ustaw jako zmienną środowiskową
# w Site settings → Environment variables.
VITE_API_URL="http://localhost:3000"
